apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    name: backend
spec:
  containers:
    - name: backend
      image: backend:1.0.0
      imagePullPolicy: Never
      ports:
        - containerPort: 80